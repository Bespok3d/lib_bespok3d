{
  "name": "@bespok3d/contract",
  "version": "0.0.0",
  "private": true,
  "description": "Shared Bespok3d SDK contract types (app<->daemon wire + main<->renderer boundary). Pure types.",
  "type": "module",
  "exports": {
    ".": "./contract/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^2.0.0"
  }
}
